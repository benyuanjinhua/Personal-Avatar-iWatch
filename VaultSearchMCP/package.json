{
  "name": "vault-search-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Obsidian Vault 只读 MCP Search（vault_search / vault_read），零外部依赖，基于 node:sqlite FTS5",
  "type": "module",
  "main": "server.mjs",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test vault-search.test.mjs"
  }
}
