{
  "name": "audio-realtime-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Audio Realtime Agent Gateway — secure WSS entry for iPhone/Watch direct realtime audio (ESS-403).",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node -e \"console.error('ESS-964 deliberate CI negative control'); process.exit(1)\""
  },
  "dependencies": {
    "ws": "^8.21.1"
  }
}
